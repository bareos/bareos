Optional and if specified set a keepalive interval (heartbeat) on the sockets with
the defined director.

If set, this value overrides :config:option:`console/console/HeartbeatInterval`\ .

See details in :ref:`section-TCP_KeepAlive`\ .
