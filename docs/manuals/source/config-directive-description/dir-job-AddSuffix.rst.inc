This directive applies only to a Restore job and specifies a suffix to all files being restored. This will use :ref:`File Relocation <filerelocation>` feature.

Using ``Add Suffix=.old``, ``/etc/passwd`` will be restored to ``/etc/passwsd.old``

