This directive specifies a directory in which the File Daemon searches for plugins with the name :file:`<pluginname>-fd.so` which it will load at startup. Typically on Linux systems, plugins are installed to :file:`/usr/lib/bareos/plugins/` or :file:`/usr/lib64/bareos/plugins/`.

