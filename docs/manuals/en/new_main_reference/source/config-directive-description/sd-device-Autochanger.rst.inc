If set to ``yes``, this device belongs to an automatic tape changer, and you must specify an Autochanger resource that points to this Device resource.

If set to ``no``, the volume must be manually changed.

In the |bareosDir|, the directive :config:option:`dir/storage/AutoChanger`\  should be set in correspondence.

