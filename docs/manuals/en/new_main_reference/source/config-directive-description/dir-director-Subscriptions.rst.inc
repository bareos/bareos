In case you want check that the number of active clients don’t exceed a specific number, you can define this number here and check with the :strong:`status subscriptions` command.

However, this is only intended to give a hint. No active limiting is implemented.

