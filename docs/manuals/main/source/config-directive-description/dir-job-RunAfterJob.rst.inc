This is a shortcut for the :config:option:`dir/job/RunScript`\  resource, that runs a command after a successful job (without error or without being canceled).

If the exit code of the program run is non-zero, Bareos will print a warning message.

