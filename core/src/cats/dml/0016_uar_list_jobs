SELECT JobId, Client.Name AS Client,
TO_CHAR(Job.StartTime, '%s') AS StartTime,
Level AS JobLevel, JobFiles, JobBytes
  FROM Client, Job
 WHERE Client.ClientId = Job.ClientId
   AND JobStatus IN ('T','W')
   AND Type            = '%c'
 ORDER BY StartTime DESC LIMIT 20
