SELECT
  SUM(unknown_mb) AS unknown_mb,
  CASE
    WHEN sum(total_mb) = 0 THEN 0
    ELSE ROUND(sum(unknown_mb) / sum(total_mb) * 100, 2)
  END AS unknown_percentage
FROM latest_full_size_categorized;
