#
# Bareos User Agent (or Console) Configuration File
#

Director {
  Name = backup-bareos-test-dir
  Port = 42001
  address = localhost
}

Console {
  Name = bareos-mon
  Description = "Restricted console used by tray-monitor to get the status of the director."
  Password = "mon_password"
}
