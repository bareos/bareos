PORTNAME=	bareos.com
PORTREVISION=	0
PKGNAMESUFFIX=	-webui-proxy
COMMENT=	Backup archiving recovery open sourced (WebUI WebSocket proxy)
PLIST=		${PKGDIR}/pkg-plist.webui-proxy
MASTERDIR=	${.CURDIR}/../bareos.com-common

# optional overrides, used by build system.
.-include "overrides.mk"

.include "${MASTERDIR}/BareosPackageOnlyMakefile"
.include "${MASTERDIR}/BareosCommonMakefile"
