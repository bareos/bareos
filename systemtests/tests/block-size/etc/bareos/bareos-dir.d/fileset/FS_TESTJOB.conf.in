FileSet {
  Name = FS_TESTJOB
  Include {
    Options {
      Signature = MD5
      xattrsupport = no           # Prevent problems on FreeBSD
    }
    File = "<@wine_drive_prefix@@tmpdir@/file-list"
  }
}
