Job {
  Name = "admin-runscript-server-failed"
  JobDefs = "DefaultJob"
  Type = Admin
  RunScript {
    Runs When = Before
    Runs On Failure = Yes
    Runs On Client = No
    Fail Job On Error = Yes
    Command = "/usr/bin/false" 
  }
}
