#
# Bareos console user for the Vue WebUI.
# Password matches the username (for test environments).
# TLS is disabled so the Python-based WS proxy can connect
# without needing TLS-PSK support.
#
Console {
  Name = admin
  Password = "admin"
  Profile = "webui-admin"
  TLS Enable = No
}
