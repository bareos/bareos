#
# Bareos User Agent (or Console) Configuration File
#

Director {
  Name = @basename@-dir
  port = @dir_port@
  Address = @hostname@
  Password = "INVALID"
}

Console {
  name = admin-tls
  password = secret
}

